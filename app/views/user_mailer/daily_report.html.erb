Hi Admin.

This is the daily report.

<% @projects.each do |project| %>
  <%= project.name%>
  <% project.find_today_tasks.each do |task| %>
    <%= task.content %> by <%= task.user.name %>
  <% end %>
<% end %>